%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% PACKAGES
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%% DIDEROT ADDITION
\usepackage[utf8]{inputenc}

%\usepackage{kpfonts}     % font
\usepackage{palatino}     % font
%\usepackage{times}     % font
\usepackage{graphicx}  % includegraphics, used heavily
\usepackage{multirow}  % Multiple rows in a table, used
\usepackage{booktabs}  % Additional table commands, used
\usepackage{colortbl}  % Add color to table entries, not used
\usepackage[usenames,dvipsnames]{xcolor} % colors, used heavily
%\usepackage{titlesec}  % changes how chapter, section, ...titles work
\usepackage{verbatim}  % allows inclusion of verbatim from file, also comment, used
%\usepackage{subfig}    % allows for \ContinuedFloat
%\usepackage[mathcal]{euscript}  % symbols for reduce etc.
\usepackage{environ}   % A better command for defining environments, used
%\usepackage{soul}      % Striking out, used
\usepackage{ifthen}    % used
\usepackage{amssymb}   % used
\usepackage{amsfonts}  % not used
\usepackage[fleqn]{amsmath}   % used, the option left justifies equations.
\usepackage{amsthm}    % used
\usepackage{mathtools} % not used (needed for xmapsto)

\usepackage{bm}        % bold math (\bm), not used
\usepackage{pifont}    % xmark, etc
\usepackage{stmaryrd}  % for \llbracket and \rrbracket
\usepackage{xspace}    % eat or add space after a command, used
\usepackage{enumitem}  % control enumerate/itemized lists, used
%\usepackage{fancyhdr}   % allows changing of headers/footers, used
\usepackage{relsize}    % change font sizes, e.g. \larger, not used

\usepackage{csquotes}    % for quotes
\usepackage{letltxmacro} % used for patching lstinline to work in math mode

\usepackage{xstring}  % check latex arguments, are they numbers etc.

\newlength\marginoriginal
\setlength{\marginoriginal}{1.5in}
\usepackage[margin=\marginoriginal]{geometry}  % used for page layout.


% xparse needed to deal with optional atom arguments
\usepackage{xparse} 

% Must come last of packages so that it can redefine various refs.
\usepackage[pdftex,
  colorlinks=true,
  linkcolor=MidnightBlue,
  urlcolor=Red
]{hyperref}


% microtype is supposed to improve typesetting, but significantly slows
% down pdflatex (by a factor of 3), so left out for now, can add back later
%\usepackage{microtype} 

\usepackage{listings}
\lstset{
 columns=[c]fixed,
}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Labels and References
%% Redefine references to allow for citation text.
%% TODO: Can do some creative things here.
%% For example
%% \let\origref\ref
%% %\renewcommand{\ref}[2][XXX]{
%% %{#1}\origref{#2}}

%% \NewDocumentCommand{\dref}{o m}
%% {
%% \IfNoValueTF{#1}
%% {\origref{#2}}
%% {{#1}\ref{#2}}
%% }


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% PAGE LAYOUT
\addtolength{\parskip}{7pt}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
